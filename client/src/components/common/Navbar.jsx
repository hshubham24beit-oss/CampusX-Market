import React from 'react';

export default function Navbar() {
  // Returning null completely removes the old horizontal top bar 
  // so your new Left Sidebar & main content layout take full effect.
  return null;
}